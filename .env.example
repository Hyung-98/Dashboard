VITE_SUPABASE_URL='YOUR_SUPABASE_URL'
VITE_SUPABASE_ANON_KEY='YOUR_SUPABASE_ANON_KEY'

# 에러 모니터링 (선택): Sentry DSN. 설정 시 ErrorBoundary 등에서 captureException.
# VITE_SENTRY_DSN='https://...@sentry.io/...'

# CAPTCHA 보호 (선택): Cloudflare Turnstile. 배포 시 봇/남용 방지용.
# 개발 환경에서는 비활성화 가능: VITE_CAPTCHA_ENABLED=false 또는 VITE_CAPTCHA_SITE_KEY 미설정
# Cloudflare Dashboard에서 Site Key 발급: https://dash.cloudflare.com/
# Supabase Dashboard → Authentication → Bot and Abuse Protection에서도 CAPTCHA 활성화 및 Secret Key 설정 필요
# VITE_CAPTCHA_SITE_KEY='YOUR_TURNSTILE_SITE_KEY'
# VITE_CAPTCHA_ENABLED='true'  # 'true' 또는 'false' (기본값: false)

# ⚠️ SERVICE_ROLE_KEY는 백엔드(Edge Function 등) 전용입니다. 클라이언트 .env에 넣지 마세요.

# 주식 시세 (선택): 미국(US) 종목 현재가 조회용. Alpha Vantage 무료 키 발급: https://www.alphavantage.co/
# 미설정 시 US 종목도 현재가/평가금액은 표시되지 않음. 한국(KR) 종목은 KIS API 연동 시 사용.
# VITE_ALPHA_VANTAGE_API_KEY='YOUR_ALPHA_VANTAGE_API_KEY'

# 한국투자증권 KIS Open API (실전/모의). 신청·발급: https://apiportal.koreainvestment.com/
# 토큰 발급·시세·주문 등 연동 시 사용. 앱시크릿은 백엔드에서만 사용 권장.
# KR 현재가: kis-kr-price Edge Function 배포 후 Supabase Dashboard → Edge Functions → Secrets 에
# KIS_APP_KEY, KIS_APP_SECRET (및 선택 시 MOK_KIS_APP_KEY, MOK_KIS_APP_SECRET) 등록 필요.
# 미배포 또는 Secrets 미설정 시 KR 종목 현재가/평가금액은 표시되지 않음.
# KIS_APP_KEY='실전투자_앱키'
# KIS_APP_SECRET='실전투자_앱시크릿'
# MOK_KIS_APP_KEY='모의투자_앱키'
# MOK_KIS_APP_SECRET='모의투자_앱시크릿'

# KIS 선택 항목 (주문/체결통보/실시간 등 사용 시)
# KIS_HTS_ID='HTS_ID_KIS_Developers_고객ID'
# KIS_ACCT_STOCK='증권계좌_앞8자리'
# KIS_ACCT_PROD='01'